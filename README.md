Monkey-patch for vagrant that shows how to tune the Vagrant SSH plugin
to work a little better when pty = true.

See ./test.sh for the test showing the patch is ok. See test-output.txt
for sample output.

